def get_values(plant) -> list:
    tmp = []
    for v in plant.values():
        tmp.append(v)
    return tmp


def get_keys(plant) -> list:
    tmp = []
    for k in plant.keys():
        tmp.append(k)
    return tmp
